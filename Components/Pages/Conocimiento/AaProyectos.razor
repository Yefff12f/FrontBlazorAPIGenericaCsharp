@page "/aa-proyectos"
@rendermode InteractiveServer
@inject FrontBlazor_AppiGenericaCsharp.Services.ApiService Api

<div class="container mt-4">
    <h3>Área Aplicación — Proyecto</h3>

    @if (!string.IsNullOrEmpty(mensaje))
    {
        <div class="alert @(exito ? "alert-success" : "alert-danger") alert-dismissible fade show">
            @mensaje
            <button type="button" class="btn-close" @onclick="() => mensaje = string.Empty"></button>
        </div>
    }

    @if (!mostrarFormulario)
    {
        <button class="btn btn-primary mb-3" @onclick="NuevoRegistro">Nueva Relación</button>
    }

    @if (mostrarFormulario)
    {
        <div class="card mb-3">
            <div class="card-header">Nueva Relación Área Aplicación — Proyecto</div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-4 mb-3">
                        <label class="form-label">ID Proyecto</label>
                        <input class="form-control" type="number" @bind="campoProyecto" />
                    </div>
                    <div class="col-md-4 mb-3">
                        <label class="form-label">ID Área Aplicación</label>
                        <input class="form-control" type="number" @bind="campoArea" />
                    </div>
                </div>
                <button class="btn btn-success me-2" @onclick="GuardarRegistro">Guardar</button>
                <button class="btn btn-secondary" @onclick="Cancelar">Cancelar</button>
            </div>
        </div>
    }

    @if (cargando)
    {
        <div class="d-flex justify-content-center my-4">
            <div class="spinner-border text-primary" role="status"></div>
        </div>
    }

    @if (!cargando && registros.Any())
    {
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr><th>Proyecto</th><th>Área Aplicación</th><th>Acciones</th></tr>
            </thead>
            <tbody>
                @foreach (var reg in registros)
                {
                    <tr>
                        <td>@reg["proyecto"]</td>
                        <td>@reg["area_aplicacion"]</td>
                        <td>
                            <button class="btn btn-danger btn-sm" @onclick="() => EliminarRegistro(reg)">Eliminar</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }

    @if (!cargando && !registros.Any())
    {
        <div class="alert alert-warning">No se encontraron registros.</div>
    }
</div>

@code {
    private List<Dictionary<string, object?>> registros = new();
    private List<Dictionary<string, object?>> Proyecto = new();
    private List<Dictionary<string, object?>> areaAplicacion = new();
    private bool cargando = true, mostrarFormulario = false, exito = false;
    private string mensaje = string.Empty;
    private int? campoProyecto = null, campoArea = null;

     protected override async Task OnInitializedAsync()
    {
        Proyecto = await Api.ListarAsync("Proyecto");          // ✅ usa variable declarada
        areaAplicacion = await Api.ListarAsync("AreaAplicacion"); // ✅ nombre consistente
        await CargarRegistros();
    }


    private async Task CargarRegistros()
    {
        cargando = true;
        registros = await Api.ListarAsync("aa_proyecto");
        cargando = false;
    }

    private void NuevoRegistro()
    {
        campoProyecto = null; campoArea = null;
        mostrarFormulario = true; mensaje = string.Empty;
    }

    private async Task GuardarRegistro()
    {
        var datos = new Dictionary<string, object?>
        {
            ["proyecto"] = campoProyecto,
            ["area_aplicacion"] = campoArea
        };
        var resultado = await Api.CrearAsync("aa_proyecto", datos);
        exito = resultado.exito; mensaje = resultado.mensaje;
        if (exito) { mostrarFormulario = false; await CargarRegistros(); }
    }

    private async Task EliminarRegistro(Dictionary<string, object?> reg)
    {
        int proy = Convert.ToInt32(reg["proyecto"]);
        int area = Convert.ToInt32(reg["area_aplicacion"]);
        var respuesta = await Api.EliminarAsync("aa_proyecto", $"{proy}", $"{area}");
        exito = respuesta.exito; mensaje = respuesta.mensaje;
        if (exito) await CargarRegistros();
    }

    private void Cancelar() { mostrarFormulario = false; mensaje = string.Empty; }
}